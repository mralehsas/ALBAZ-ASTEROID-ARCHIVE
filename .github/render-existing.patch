diff --git a/database.py b/database.py
index 9a9a83f..81ab2ac 100644
--- a/database.py
+++ b/database.py
@@ -15,8 +15,7 @@ import sqlite3
 from pathlib import Path
 from typing import Any, Iterable
 
-ROOT = Path(__file__).resolve().parent
-DB_PATH = ROOT / "data" / "asteroid_archive.db"
+from runtime_paths import BUNDLE_DATA_DIR, DB_PATH
 VALID_DATASETS = {"approaches", "fireballs", "sentry", "objects", "meteorites", "impact_structures"}
 
 
@@ -681,7 +680,7 @@ def seed_if_empty(*, path: Path = DB_PATH) -> bool:
     changed = False
     current = counts(path=path)
 
-    bootstrap = ROOT / "data" / "bootstrap.json"
+    bootstrap = BUNDLE_DATA_DIR / "bootstrap.json"
     if bootstrap.exists() and not (current.get("approaches") or current.get("fireballs") or current.get("sentry")):
         try:
             payload = json.loads(bootstrap.read_text(encoding="utf-8"))
@@ -695,7 +694,7 @@ def seed_if_empty(*, path: Path = DB_PATH) -> bool:
             set_metadata("bootstrap_note", payload.get("note") or "bundled reference data", path=path)
             changed = True
 
-    earth_history = ROOT / "data" / "earth_history.json"
+    earth_history = BUNDLE_DATA_DIR / "earth_history.json"
     if earth_history.exists() and not (current.get("meteorites") and current.get("impact_structures")):
         try:
             payload = json.loads(earth_history.read_text(encoding="utf-8"))
diff --git a/horizons_engine.py b/horizons_engine.py
index 42f26b8..7841b1d 100644
--- a/horizons_engine.py
+++ b/horizons_engine.py
@@ -19,9 +19,9 @@ from pathlib import Path
 from typing import Any, Final
 
 from jpl_client import ensure_signature, network_error_label, request_json, signature_info
+from runtime_paths import HORIZONS_CACHE_DIR
 
-ROOT: Final[Path] = Path(__file__).resolve().parent
-CACHE_DIR: Final[Path] = ROOT / "data" / "horizons_cache"
+CACHE_DIR: Final[Path] = HORIZONS_CACHE_DIR
 VERSION: Final[str] = "0.7.2"
 HORIZONS_URL: Final[str] = "https://ssd.jpl.nasa.gov/api/horizons.api"
 LOOKUP_URL: Final[str] = "https://ssd.jpl.nasa.gov/api/horizons_lookup.api"
diff --git a/index.html b/index.html
index b09acd5..66ded07 100644
--- a/index.html
+++ b/index.html
@@ -1692,6 +1692,7 @@ window.AA_EARTH_HISTORY = {"generated_at":"2026-07-03T00:00:00Z","note_ar":"مج
 window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
 
 </script>
+  <script src="./web-config.js"></script>
   <script>
 (() => {
   'use strict';
@@ -2005,6 +2006,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
     smartMap: { viewMode: '2d', layer: 'fireballs', centerLon: 0, centerLat: 18, zoom: 1, showGrid: true, showReference: true, showLabels: true, pointerLat: null, pointerLon: null, gl: null, program: null, buffers: null, texture: null, imageReady: false, drag: null },
     cinematic: { step: 0, playing: false, timer: null },
     sourceMode: 'sample',
+    backendReachable: false,
     databaseReady: false,
     closeApproaches: [],
     fireballs: [],
@@ -2055,29 +2057,47 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
 
   function todayIso() { return new Date().toISOString().slice(0, 10); }
   function addYearsIso(years) { const d = new Date(); d.setUTCFullYear(d.getUTCFullYear() + years); return d.toISOString().slice(0, 10); }
-  function isLocalServer() { return ['127.0.0.1', 'localhost'].includes(location.hostname) && ['http:', 'https:'].includes(location.protocol); }
+  function isLocalServer() {
+    return ['127.0.0.1', 'localhost'].includes(location.hostname) && ['http:', 'https:'].includes(location.protocol);
+  }
+
+  function apiBaseUrl() {
+    if (isLocalServer()) return location.origin;
+    const raw = String(window.ALBAZ_WEB_CONFIG?.apiBaseUrl || '').trim();
+    return raw.replace(/\/+$/, '');
+  }
+
+  function backendApiAvailable() {
+    return isLocalServer() || Boolean(apiBaseUrl());
+  }
 
   function endpoint(kind, params = {}) {
-    const remote = {
-      cad: 'https://ssd-api.jpl.nasa.gov/cad.api',
-      fireball: 'https://ssd-api.jpl.nasa.gov/fireball.api',
-      sbdb: 'https://ssd-api.jpl.nasa.gov/sbdb.api',
-      sentry: 'https://ssd-api.jpl.nasa.gov/sentry.api',
+    const routes = {
+      cad: '/api/cad',
+      fireball: '/api/fireball',
+      sbdb: '/api/sbdb',
+      sentry: '/api/sentry',
       health: '/api/health',
       localApproaches: '/api/local/approaches',
       localFireballs: '/api/local/fireballs',
       localSentry: '/api/local/sentry',
       localMeteorites: '/api/local/meteorites',
       localImpacts: '/api/local/impact-structures',
-      object: '/api/object', updateStatus: '/api/update/status', updateHistory: '/api/update/history',
-      updateStart: '/api/update/start', updateCancel: '/api/update/cancel', connectivity: '/api/connectivity/test', horizonsTrack: '/api/horizons/track'
+      object: '/api/object',
+      updateStatus: '/api/update/status',
+      updateHistory: '/api/update/history',
+      updateStart: '/api/update/start',
+      updateCancel: '/api/update/cancel',
+      connectivity: '/api/connectivity/test',
+      horizonsTrack: '/api/horizons/track'
     };
-    const localProxy = { cad: '/api/cad', fireball: '/api/fireball', sbdb: '/api/sbdb', sentry: '/api/sentry' };
-    let base = remote[kind];
-    if (isLocalServer() && localProxy[kind]) base = localProxy[kind];
+    const route = routes[kind];
+    if (!route) throw new Error(`Unknown API endpoint: ${kind}`);
+    const origin = apiBaseUrl();
+    const base = origin ? `${origin}${route}` : route;
     const url = new URL(base, location.href);
-    Object.entries(params).forEach(([k, v]) => {
-      if (v !== undefined && v !== null && v !== '') url.searchParams.set(k, String(v));
+    Object.entries(params).forEach(([key, value]) => {
+      if (value !== undefined && value !== null && value !== '') url.searchParams.set(key, String(value));
     });
     return url.toString();
   }
@@ -2163,9 +2183,15 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   function updateConnectionUi() {
     const pill = $('#connectionPill');
     pill.classList.remove('online', 'offline');
+    const backendMode = isLocalServer() ? 'local' : state.backendReachable ? 'backend' : 'offline';
+    const backendLabel = backendMode === 'local'
+      ? 'Local Desktop Mode'
+      : backendMode === 'backend'
+        ? 'Backend Online'
+        : 'Backend Offline';
+    pill.classList.add(backendMode === 'offline' ? 'offline' : 'online');
+    $('#connectionText').textContent = backendLabel;
     const live = state.sourceMode === 'live';
-    pill.classList.add(live ? 'online' : 'offline');
-    $('#connectionText').textContent = live ? t('online') : t('offline');
     const sourceText = live ? t('sourceLive') : state.sourceMode === 'local' ? t('sourceLocal') : t('sourceSample');
     $('#sourceModeValue').textContent = sourceText;
     $('#databaseStateValue').textContent = state.databaseReady ? t('dbReady') : t('dbUnavailable');
@@ -2179,9 +2205,16 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   }
 
   async function checkHealth() {
-    if (!isLocalServer()) return false;
+    const base = apiBaseUrl();
+    if (!isLocalServer() && !base) {
+      state.backendReachable = false;
+      state.databaseReady = false;
+      updateConnectionUi();
+      return false;
+    }
     try {
       state.health = await fetchJson(endpoint('health'), 5000);
+      state.backendReachable = true;
       state.databaseReady = Boolean(state.health.database_ready);
       state.dbCounts = state.health.counts || {};
       state.updateHistory = state.health.recent_updates || [];
@@ -2190,13 +2223,14 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
       renderUpdateHistory();
       return true;
     } catch (_) {
+      state.backendReachable = false;
       state.databaseReady = false;
       return false;
     } finally { updateConnectionUi(); }
   }
 
   async function loadLocalDataset(kind) {
-    if (!isLocalServer()) return [];
+    if (!backendApiAvailable()) return [];
     try {
       const payload = await fetchJson(endpoint(kind), 8000);
       return parseArrayPayload(payload).map(row => ({ ...row, source_mode: 'local' }));
@@ -4662,7 +4696,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   }
 
   async function refreshUpdateHistory() {
-    if (!isLocalServer()) { state.updateHistory = []; renderUpdateHistory(); return; }
+    if (!backendApiAvailable()) { state.updateHistory = []; renderUpdateHistory(); return; }
     try {
       const payload = await fetchJson(endpoint('updateHistory', { limit: 20 }), 8000);
       state.updateHistory = Array.isArray(payload.records) ? payload.records : [];
@@ -4671,7 +4705,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   }
 
   async function refreshNasaStatus() {
-    if (!isLocalServer()) { renderEngineState({ status:'idle', running:false, message:t('engineLocalOnly'), percent:0, logs:[] }); return; }
+    if (!backendApiAvailable()) { renderEngineState({ status:'idle', running:false, message:t('engineLocalOnly'), percent:0, logs:[] }); return; }
     try {
       const snapshot = await fetchJson(endpoint('updateStatus'), 8000);
       const wasRunning = Boolean(state.engineState?.running);
@@ -4702,7 +4736,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
 
   async function startNasaUpdate(event) {
     event?.preventDefault();
-    if (!isLocalServer()) { toast(t('engineLocalOnly'), 'error'); return; }
+    if (!backendApiAvailable()) { toast(t('engineLocalOnly'), 'error'); return; }
     try {
       const payload = await postJson(endpoint('updateStart'), engineConfig());
       state.engineState = payload.state || { status:'running', running:true, percent:0, logs:[] };
@@ -4714,7 +4748,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   }
 
   async function cancelNasaUpdate() {
-    if (!isLocalServer()) return;
+    if (!backendApiAvailable()) return;
     try {
       await postJson(endpoint('updateCancel'), {});
       toast(t('cancelRequested'));
@@ -4736,9 +4770,9 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
     state.connectivity = payload;
     const localCard = $('[data-service="local"]');
     if (localCard) {
-      localCard.classList.toggle('online', isLocalServer());
-      localCard.classList.toggle('offline', !isLocalServer());
-      $('strong', localCard).textContent = isLocalServer() ? t('localReady') : t('serviceOffline');
+      localCard.classList.toggle('online', state.databaseReady);
+      localCard.classList.toggle('offline', !state.databaseReady);
+      $('strong', localCard).textContent = state.databaseReady ? t('localReady') : t('serviceOffline');
     }
     const services = payload?.services || {};
     Object.entries(services).forEach(([name, item]) => {
@@ -4764,7 +4798,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
   }
 
   async function testNasaConnection() {
-    if (!isLocalServer()) { toast(t('engineLocalOnly'), 'error'); return; }
+    if (!backendApiAvailable()) { toast(t('engineLocalOnly'), 'error'); return; }
     const button = $('#testNasaConnectionButton');
     button.disabled = true;
     setServiceCardsChecking();
@@ -4875,7 +4909,7 @@ window.AA_LIVE_CACHE = window.AA_LIVE_CACHE || null;
 
   async function loadHorizonsTrack(event) {
     event?.preventDefault();
-    if (!isLocalServer()) { toast(t('engineLocalOnly'), 'error'); return; }
+    if (!backendApiAvailable()) { toast(t('engineLocalOnly'), 'error'); return; }
     stopOrbitPlayback();
     setOrbitLoading(true);
     try {
diff --git a/server.py b/server.py
index f874230..39b36de 100644
--- a/server.py
+++ b/server.py
@@ -10,6 +10,7 @@ from __future__ import annotations
 import argparse
 import json
 import mimetypes
+import os
 import threading
 import time
 import urllib.error
@@ -64,6 +65,33 @@ LOCAL_DATASETS: Final[dict[str, str]] = {
 }
 CACHE_TTL_SECONDS: Final[int] = 300
 CACHE: dict[str, tuple[float, bytes, str]] = {}
+PAGES_ORIGIN: Final[str] = "https://mralehsas.github.io"
+
+
+def environment_default_host() -> str:
+    return str(os.environ.get("HOST") or "127.0.0.1").strip() or "127.0.0.1"
+
+
+def environment_default_port() -> int:
+    raw = str(os.environ.get("PORT") or "8872").strip()
+    try:
+        port = int(raw)
+    except ValueError:
+        return 8872
+    return port if 1 <= port <= 65535 else 8872
+
+
+def allowed_cors_origin(origin: str | None) -> str | None:
+    value = str(origin or "").strip()
+    if value == PAGES_ORIGIN:
+        return value
+    try:
+        parsed = urllib.parse.urlsplit(value)
+    except ValueError:
+        return None
+    if parsed.scheme in {"http", "https"} and parsed.hostname in {"127.0.0.1", "localhost"}:
+        return value
+    return None
 
 UPDATE_LOCK = threading.RLock()
 UPDATE_CANCEL = threading.Event()
@@ -136,8 +164,18 @@ class ArchiveHandler(SimpleHTTPRequestHandler):
         self.send_header("Pragma", "no-cache")
         self.send_header("Expires", "0")
         self.send_header("X-Asteroid-Archive-Version", VERSION)
+        origin = allowed_cors_origin(self.headers.get("Origin"))
+        if origin:
+            self.send_header("Access-Control-Allow-Origin", origin)
+            self.send_header("Vary", "Origin")
+            self.send_header("Access-Control-Allow-Methods", "GET, POST, OPTIONS")
+            self.send_header("Access-Control-Allow-Headers", "Content-Type")
         super().end_headers()
 
+    def do_OPTIONS(self) -> None:  # noqa: N802
+        self.send_response(HTTPStatus.NO_CONTENT)
+        self.end_headers()
+
     def do_GET(self) -> None:  # noqa: N802
         parsed = urllib.parse.urlsplit(self.path)
         if parsed.path in REMOTE_ENDPOINTS:
@@ -373,6 +411,12 @@ class ArchiveHandler(SimpleHTTPRequestHandler):
                 "cache_entries": len(CACHE),
                 "update": update_snapshot(),
                 "recent_updates": recent_update_runs(limit=3),
+                "runtime": {
+                    "host_default": environment_default_host(),
+                    "port_default": environment_default_port(),
+                    "data_dir": str(DB_PATH.parent),
+                    "external_data_dir": bool(str(os.environ.get("ALBAZ_DATA_DIR") or "").strip()),
+                },
             }
         )
 
@@ -401,8 +445,8 @@ class ArchiveHandler(SimpleHTTPRequestHandler):
 
 def main() -> int:
     parser = argparse.ArgumentParser(description="Run Asteroid Archive v0.7 Final Audited Edition with SQLite, serialized NASA/JPL access, Horizons and comparison.")
-    parser.add_argument("--host", default="127.0.0.1", help="Bind host (default: 127.0.0.1)")
-    parser.add_argument("--port", type=int, default=8872, help="Bind port (default: 8872; reserved for Asteroid Archive)")
+    parser.add_argument("--host", default=environment_default_host(), help="Bind host")
+    parser.add_argument("--port", type=int, default=environment_default_port(), help="Bind port")
     parser.add_argument("--no-open", action="store_true", help="Do not open the browser automatically")
     args = parser.parse_args()
 
@@ -412,7 +456,9 @@ def main() -> int:
     requested_port = int(args.port)
     server = None
     last_error = None
-    for candidate_port in range(requested_port, requested_port + 21):
+    render_managed_port = bool(str(os.environ.get("PORT") or "").strip())
+    port_candidates = [requested_port] if render_managed_port else range(requested_port, requested_port + 21)
+    for candidate_port in port_candidates:
         try:
             server = ThreadingHTTPServer((args.host, candidate_port), ArchiveHandler)
             args.port = candidate_port
diff --git a/update_engine.py b/update_engine.py
index fb6b2ad..e99bc1b 100644
--- a/update_engine.py
+++ b/update_engine.py
@@ -25,9 +25,9 @@ from database import (
     remove_database_backup,
 )
 from jpl_client import ensure_signature, request_json, signature_info
+from runtime_paths import LIVE_CACHE_PATH
 
-ROOT: Final[Path] = Path(__file__).resolve().parent
-CACHE_PATH: Final[Path] = ROOT / "data" / "live-cache.js"
+CACHE_PATH: Final[Path] = LIVE_CACHE_PATH
 VERSION: Final[str] = "0.7.2"
 
 ProgressCallback = Callable[[dict[str, Any]], None]
